{
  "name": "pixel-idle-game",
  "version": "0.0.0",
  "private": true,
  "description": "Pixel idle MMORPG de browser (Tibia-like): party de 4 classes, mundo em biomas, loot + marketplace.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @pixel-idle/web",
    "dev:server": "npm run dev -w @pixel-idle/server",
    "build": "npm run build -w @pixel-idle/web"
  }
}
